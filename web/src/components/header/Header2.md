Basic Header:
```jsx harmony
import { faClinicMedical } from '@fortawesome/free-solid-svg-icons/faClinicMedical';
import Icon from '../Icon/Icon';

const logo = (
  <Icon icon={faClinicMedical} size={'lg'} style={{ color: '#fff' }} />
);

<Header2
  logoRedirectToHome={true}
  logo={logo}
  title={'GNet'}
/>
```